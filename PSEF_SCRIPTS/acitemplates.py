def aci_create_filter (tenant, name, prot, ports):
    config_txt = ''
    return (config_txt)

def aci_delete_filter (tenant, name, prot, ports):
    config_txt = ''
    return (config_txt)

def aci_create_subject (tenant, name, filter_list):
    config_txt = ''
    return (config_txt)

def aci_delete_subject (tenant, name, filter_list):
    config_txt = ''
    return (config_txt)

def aci_create_contract (tenant, name, subject_list, subject_dict, action ):
    config_txt = ''
    return (config_txt)

def aci_delete_contract (tenant, name, subject_list, subject_dict, action ):
    config_txt = ''
    return (config_txt)

def aci_attach_epg (tenant, name, epg_list_cons, subject_list, action ):
    config_txt = ''
    return (config_txt)

def aci_deattach_epg (tenant, name, epg_list_cons, subject_list, action ):
    config_txt = ''
    return (config_txt)

def aci_create_policy (tenant, name, epg_list_cons, subject_list, action ):
    config_txt = ''
    return (config_txt)

def aci_delete_policy (tenant, name, epg_list_cons, subject_list, action ):
    config_txt = ''
    return (config_txt)
